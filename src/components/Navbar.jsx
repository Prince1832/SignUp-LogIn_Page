import HomeButton from './HomeButton';
import BackButton from './BackButton';

const Navbar = () => {
  return (
    <div>
      <HomeButton />
      <BackButton />
    </div>
  );
};

export default Navbar;
